# Pennylane Tools - Allowlist
# Curated subset of Pennylane API for AI agent use.
# OAuth2 via Nango.
# Each tool is an allowlist entry: only listed tools are exposed to LLMs.

# === TOOL LIST ===
# 1. pennylane_list_accounts            - List accounts
# 2. pennylane_get_account              - Get account details
# 3. pennylane_list_contacts            - List contacts
# 4. pennylane_get_contact             - Get contact details
# 5. pennylane_list_invoices           - List invoices
# 6. pennylane_get_invoice             - Get invoice details
# 7. pennylane_list_expenses           - List expenses
# 8. pennylane_get_expense             - Get expense details
# 9. pennylane_list_items              - List items/products
# 10. pennylane_get_company_info       - Get company information

- slug: pennylane_list_accounts
  name: List pennylane Accounts
  description: |
    Retrieve all accounts from the accounting system including account names, types, and balance information.
  provider: pennylane
  endpoint: /api/v1/accounts
  method: GET
  scopes: []
  tags:
    - accounts
    - accounting
    - list
  input_schema:
    type: object
    description: Parameters for listing accounts
    properties:
      type:
        type: string
        description: "Filter by account type (asset, liability, equity, revenue, expense)"
    required: []

- slug: pennylane_get_account
  name: Get pennylane Account
  description: |
    Retrieve detailed information about a specific account including its balance, type, and transaction history.
  provider: pennylane
  endpoint: /api/v1/accounts/{accountId}
  method: GET
  scopes: []
  tags:
    - accounts
    - accounting
    - read
  input_schema:
    type: object
    description: Parameters for getting an account
    properties:
      accountId:
        type: string
        description: "Account ID"
    required:
      - accountId

- slug: pennylane_list_contacts
  name: List pennylane Contacts
  description: |
    Retrieve all contacts including customers and suppliers with names, emails, and payment information.
  provider: pennylane
  endpoint: /api/v1/contacts
  method: GET
  scopes: []
  tags:
    - contacts
    - crm
    - list
  input_schema:
    type: object
    description: Parameters for listing contacts
    properties:
      type:
        type: string
        description: "Filter by type (customer, supplier)"
    required: []

- slug: pennylane_get_contact
  name: Get pennylane Contact
  description: |
    Retrieve detailed information about a specific contact including their details, payment terms, and transaction history.
  provider: pennylane
  endpoint: /api/v1/contacts/{contactId}
  method: GET
  scopes: []
  tags:
    - contacts
    - crm
    - read
  input_schema:
    type: object
    description: Parameters for getting a contact
    properties:
      contactId:
        type: string
        description: "Contact ID"
    required:
      - contactId

- slug: pennylane_list_invoices
  name: List pennylane Invoices
  description: |
    Retrieve all invoices including invoice numbers, amounts, status, and associated contact information.
  provider: pennylane
  endpoint: /api/v1/invoices
  method: GET
  scopes: []
  tags:
    - invoices
    - billing
    - list
  input_schema:
    type: object
    description: Parameters for listing invoices
    properties:
      status:
        type: string
        description: "Filter by status (draft, sent, paid, cancelled)"
    required: []

- slug: pennylane_get_invoice
  name: Get pennylane Invoice
  description: |
    Retrieve detailed information about a specific invoice including line items, totals, and payment status.
  provider: pennylane
  endpoint: /api/v1/invoices/{invoiceId}
  method: GET
  scopes: []
  tags:
    - invoices
    - billing
    - read
  input_schema:
    type: object
    description: Parameters for getting an invoice
    properties:
      invoiceId:
        type: string
        description: "Invoice ID"
    required:
      - invoiceId

- slug: pennylane_list_expenses
  name: List pennylane Expenses
  description: |
    Retrieve all expenses including amounts, dates, categories, and associated receipts.
  provider: pennylane
  endpoint: /api/v1/expenses
  method: GET
  scopes: []
  tags:
    - expenses
    - accounting
    - list
  input_schema:
    type: object
    description: Parameters for listing expenses
    properties:
      status:
        type: string
        description: "Filter by status (draft, validated)"
      startDate:
        type: string
        description: "Start date filter (YYYY-MM-DD)"
      endDate:
        type: string
        description: "End date filter (YYYY-MM-DD)"
    required: []

- slug: pennylane_get_expense
  name: Get pennylane Expense
  description: |
    Retrieve detailed information about a specific expense including its category, amount, date, and receipt.
  provider: pennylane
  endpoint: /api/v1/expenses/{expenseId}
  method: GET
  scopes: []
  tags:
    - expenses
    - accounting
    - read
  input_schema:
    type: object
    description: Parameters for getting an expense
    properties:
      expenseId:
        type: string
        description: "Expense ID"
    required:
      - expenseId

- slug: pennylane_list_items
  name: List Items
  description: |
    Retrieve all products and services in the catalog including names, prices, and SKU information.
  provider: pennylane
  endpoint: /api/v1/items
  method: GET
  scopes: []
  tags:
    - items
    - products
    - list
  input_schema:
    type: object
    description: Parameters for listing items
    properties: {}
    required: []

- slug: pennylane_get_company_info
  name: Get Company Info
  description: |
    Retrieve company/organization information including name, address, fiscal settings, and tax identification.
  provider: pennylane
  endpoint: /api/v1/company
  method: GET
  scopes: []
  tags:
    - company
    - organization
    - read
  input_schema:
    type: object
    description: Parameters for getting company info
    properties: {}
    required: []
