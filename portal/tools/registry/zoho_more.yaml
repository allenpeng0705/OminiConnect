# Zoho More Tools - Allowlist
# Curated subset of additional Zoho services for AI agent use.
# Each tool is an allowlist entry: only listed tools are exposed to LLMs.

# === TOOL LIST ===
# 1. zoho_list_invoices          - List invoices from Zoho Books
# 2. zoho_get_invoice           - Get invoice details
# 3. zoho_create_invoice        - Create a new invoice
# 4. zoho_list_contacts         - List contacts from Zoho People
# 5. zoho_get_contact           - Get contact details
# 6. zoho_list_items            - List items from Zoho Inventory
# 7. zoho_get_item              - Get item details
# 8. zoho_list_purchase_orders  - List purchase orders
# 9. zoho_get_purchase_order    - Get purchase order details
# 10. zoho_list_employees       - List employees from Zoho People

- slug: zoho_list_invoices
  name: List zoho_more Invoices
  description: |
    Retrieve all invoices from Zoho Books with optional filtering by status, customer, or date range. Returns invoice numbers, amounts, statuses, and customer information.
  provider: zoho_more
  endpoint: /books/v3/invoices
  method: GET
  scopes:
    - zoho_books.invoices.read
  tags:
    - invoices
    - billing
    - books
  input_schema:
    type: object
    description: Parameters for listing invoices
    properties:
      status:
        type: string
        description: "Filter by status: draft, sent, paid, overdue, void"
        enum: [draft, sent, paid, overdue, void]
      customer_id:
        type: string
        description: "Filter by customer ID"
      date_after:
        type: string
        description: "Filter by date (YYYY-MM-DD)"
      date_before:
        type: string
        description: "Filter by date (YYYY-MM-DD)"
      page:
        type: integer
        description: "Page number"
        default: 1
      per_page:
        type: integer
        description: "Results per page"
        default: 25
    required: []

- slug: zoho_get_invoice
  name: Get zoho_more Invoice
  description: |
    Retrieve detailed information for a specific invoice by its unique ID. Returns line items, taxes, payment terms, total amount, and settlement status.
  provider: zoho_more
  endpoint: /books/v3/invoices/{invoice_id}
  method: GET
  scopes:
    - zoho_books.invoices.read
  tags:
    - invoices
    - billing
    - read
  input_schema:
    type: object
    description: Parameters for getting an invoice
    properties:
      invoice_id:
        type: string
        description: "Invoice ID (path parameter)"
    required:
      - invoice_id

- slug: zoho_create_invoice
  name: Create Invoice
  description: |
    Create a new invoice in Zoho Books with specified customer, line items, tax, and payment terms. Returns the created invoice with its unique ID.
  provider: zoho_more
  endpoint: /books/v3/invoices
  method: POST
  scopes:
    - zoho_books.invoices.create
  tags:
    - invoices
    - billing
    - create
  input_schema:
    type: object
    description: Invoice creation parameters
    properties:
      customer_id:
        type: string
        description: "Customer ID (required)"
      invoice_number:
        type: string
        description: "Invoice number (auto-generated if not provided)"
      date:
        type: string
        description: "Invoice date (YYYY-MM-DD)"
      due_date:
        type: string
        description: "Payment due date (YYYY-MM-DD)"
      line_items:
        type: array
        items:
          type: object
        description: "Array of line items: {item_id, quantity, rate}"
      notes:
        type: string
        description: "Additional notes or terms"
    required:
      - customer_id

- slug: zoho_list_contacts
  name: List zoho_more Contacts
  description: |
    Retrieve all contacts from Zoho People with optional filtering by department or designation. Returns contact names, email addresses, and employment information.
  provider: zoho_more
  endpoint: /people/v3/contacts
  method: GET
  scopes:
    - zoho_people.contacts.read
  tags:
    - contacts
    - employees
    - people
  input_schema:
    type: object
    description: Parameters for listing contacts
    properties:
      department:
        type: string
        description: "Filter by department"
      designation:
        type: string
        description: "Filter by job title"
      search:
        type: string
        description: "Search by name or email"
      page:
        type: integer
        description: "Page number"
        default: 1
      per_page:
        type: integer
        description: "Results per page"
        default: 25
    required: []

- slug: zoho_get_contact
  name: Get zoho_more Contact
  description: |
    Retrieve detailed information for a specific contact by their unique ID. Returns employment details, reporting structure, and contact information.
  provider: zoho_more
  endpoint: /people/v3/contacts/{contact_id}
  method: GET
  scopes:
    - zoho_people.contacts.read
  tags:
    - contacts
    - employees
    - read
  input_schema:
    type: object
    description: Parameters for getting a contact
    properties:
      contact_id:
        type: string
        description: "Contact ID (path parameter)"
    required:
      - contact_id

- slug: zoho_list_items
  name: List Items
  description: |
    Retrieve all items from Zoho Inventory with optional filtering by name, category, or status. Returns item names, prices, stock levels, and categories.
  provider: zoho_more
  endpoint: /inventory/v3/items
  method: GET
  scopes:
    - zoho_inventory.items.read
  tags:
    - items
    - products
    - inventory
  input_schema:
    type: object
    description: Parameters for listing items
    properties:
      name:
        type: string
        description: "Filter by item name"
      category:
        type: string
        description: "Filter by category"
      status:
        type: string
        description: "Filter by status: active, inactive"
        enum: [active, inactive]
      page:
        type: integer
        description: "Page number"
        default: 1
      per_page:
        type: integer
        description: "Results per page"
        default: 25
    required: []

- slug: zoho_get_item
  name: Get Item
  description: |
    Retrieve detailed information for a specific item by its unique ID. Returns pricing details, stock levels, and custom fields.
  provider: zoho_more
  endpoint: /inventory/v3/items/{item_id}
  method: GET
  scopes:
    - zoho_inventory.items.read
  tags:
    - items
    - products
    - read
  input_schema:
    type: object
    description: Parameters for getting an item
    properties:
      item_id:
        type: string
        description: "Item ID (path parameter)"
    required:
      - item_id

- slug: zoho_list_purchase_orders
  name: List Purchase Orders
  description: |
    Retrieve all purchase orders from Zoho Inventory with optional filtering by status or vendor. Returns purchase order numbers, vendor info, and status.
  provider: zoho_more
  endpoint: /inventory/v3/purchaseorders
  method: GET
  scopes:
    - zoho_inventory.purchaseorders.read
  tags:
    - purchaseorders
    - procurement
    - inventory
  input_schema:
    type: object
    description: Parameters for listing purchase orders
    properties:
      status:
        type: string
        description: "Filter by status: draft, confirmed, partially_received, received, cancelled"
      vendor_id:
        type: string
        description: "Filter by vendor ID"
      page:
        type: integer
        description: "Page number"
        default: 1
      per_page:
        type: integer
        description: "Results per page"
        default: 25
    required: []

- slug: zoho_get_purchase_order
  name: Get Purchase Order
  description: |
    Retrieve detailed information for a specific purchase order by its unique ID. Returns line items, vendor details, and delivery status.
  provider: zoho_more
  endpoint: /inventory/v3/purchaseorders/{purchase_order_id}
  method: GET
  scopes:
    - zoho_inventory.purchaseorders.read
  tags:
    - purchaseorders
    - procurement
    - read
  input_schema:
    type: object
    description: Parameters for getting a purchase order
    properties:
      purchase_order_id:
        type: string
        description: "Purchase Order ID (path parameter)"
    required:
      - purchase_order_id

- slug: zoho_list_employees
  name: List Employees
  description: |
    Retrieve all employees from Zoho People with optional filtering by department or status. Returns employee names, departments, designations, and employment status.
  provider: zoho_more
  endpoint: /people/v3/employees
  method: GET
  scopes:
    - zoho_people.employees.read
  tags:
    - employees
    - hr
    - people
  input_schema:
    type: object
    description: Parameters for listing employees
    properties:
      department:
        type: string
        description: "Filter by department"
      status:
        type: string
        description: "Filter by status: active, inactive"
        enum: [active, inactive]
      search:
        type: string
        description: "Search by name or email"
      page:
        type: integer
        description: "Page number"
        default: 1
      per_page:
        type: integer
        description: "Results per page"
        default: 25
    required: []