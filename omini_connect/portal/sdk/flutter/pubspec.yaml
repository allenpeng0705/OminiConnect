name: ominiconnect
description: OminiConnect Flutter SDK — connect AI agents to external platforms.
version: 0.1.0
repository: https://github.com/allenpeng0705/OminiConnect

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  http: '>=1.0.0 <2.0.0'

dev_dependencies:
  flutter_test:
  flutter_lints: ^3.0.0
