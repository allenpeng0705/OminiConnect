# Pendo Tools - Allowlist
# Curated subset of Pendo REST API for AI agent use.
# Each tool is an allowlist entry: only listed tools are exposed to LLMs.

# === TOOL LIST ===
# 1. pendo_list_features       - List all features
# 2. pendo_get_feature         - Get feature details
# 3. pendo_list_events         - List events
# 4. pendo_get_event           - Get event details
# 5. pendo_list_pages          - List pages
# 6. pendo_get_page            - Get page details
# 7. pendo_list_guides         - List guides
# 8. pendo_get_guide           - Get guide details
# 9. pendo_list_users          - List users
# 10. pendo_get_user           - Get user details

- slug: pendo_list_features
  name: List Features
  description: |
    List all features in your Pendo product.
  provider: pendo
  endpoint: /api/v1/features
  method: GET
  scopes:
    - features:read
  tags:
    - features
    - list
  input_schema:
    type: object
    description: Parameters for listing features
    properties:
      productId:
        type: string
        description: "Product ID to filter by"
    required: []

- slug: pendo_get_feature
  name: Get Feature
  description: |
    Get detailed information about a specific feature.
  provider: pendo
  endpoint: /api/v1/features/{featureId}
  method: GET
  scopes:
    - features:read
  tags:
    - features
    - read
  input_schema:
    type: object
    description: Parameters for getting a feature
    properties:
      featureId:
        type: string
        description: "Feature ID"
    required:
      - featureId

- slug: pendo_list_events
  name: List Events
  description: |
    List all events tracked in your Pendo product.
  provider: pendo
  endpoint: /api/v1/events
  method: GET
  scopes:
    - events:read
  tags:
    - events
    - list
  input_schema:
    type: object
    description: Parameters for listing events
    properties:
      productId:
        type: string
        description: "Product ID to filter by"
      pageSize:
        type: integer
        description: "Number of results (default 50, max 200)"
        default: 50
    required: []

- slug: pendo_get_event
  name: Get Event
  description: |
    Get detailed information about a specific event.
  provider: pendo
  endpoint: /api/v1/events/{eventId}
  method: GET
  scopes:
    - events:read
  tags:
    - events
    - read
  input_schema:
    type: object
    description: Parameters for getting an event
    properties:
      eventId:
        type: string
        description: "Event ID"
    required:
      - eventId

- slug: pendo_list_pages
  name: List Pages
  description: |
    List all pages in your Pendo product.
  provider: pendo
  endpoint: /api/v1/pages
  method: GET
  scopes:
    - pages:read
  tags:
    - pages
    - list
  input_schema:
    type: object
    description: Parameters for listing pages
    properties:
      productId:
        type: string
        description: "Product ID to filter by"
    required: []

- slug: pendo_get_page
  name: Get Page
  description: |
    Get detailed information about a specific page.
  provider: pendo
  endpoint: /api/v1/pages/{pageId}
  method: GET
  scopes:
    - pages:read
  tags:
    - pages
    - read
  input_schema:
    type: object
    description: Parameters for getting a page
    properties:
      pageId:
        type: string
        description: "Page ID"
    required:
      - pageId

- slug: pendo_list_guides
  name: List Guides
  description: |
    List all guides in your Pendo product.
  provider: pendo
  endpoint: /api/v1/guides
  method: GET
  scopes:
    - guides:read
  tags:
    - guides
    - list
  input_schema:
    type: object
    description: Parameters for listing guides
    properties:
      productId:
        type: string
        description: "Product ID to filter by"
      status:
        type: string
        description: "Filter by status (active, archived, draft)"
    required: []

- slug: pendo_get_guide
  name: Get Guide
  description: |
    Get detailed information about a specific guide.
  provider: pendo
  endpoint: /api/v1/guides/{guideId}
  method: GET
  scopes:
    - guides:read
  tags:
    - guides
    - read
  input_schema:
    type: object
    description: Parameters for getting a guide
    properties:
      guideId:
        type: string
        description: "Guide ID"
    required:
      - guideId

- slug: pendo_list_users
  name: List Users
  description: |
    List all users in your Pendo product.
  provider: pendo
  endpoint: /api/v1/users
  method: GET
  scopes:
    - users:read
  tags:
    - users
    - list
  input_schema:
    type: object
    description: Parameters for listing users
    properties:
      productId:
        type: string
        description: "Product ID to filter by"
      pageSize:
        type: integer
        description: "Number of results (default 50, max 200)"
        default: 50
    required: []

- slug: pendo_get_user
  name: Get User
  description: |
    Get detailed information about a specific user.
  provider: pendo
  endpoint: /api/v1/users/{userId}
  method: GET
  scopes:
    - users:read
  tags:
    - users
    - read
  input_schema:
    type: object
    description: Parameters for getting a user
    properties:
      userId:
        type: string
        description: "User ID"
    required:
      - userId
