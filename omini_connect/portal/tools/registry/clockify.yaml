# Clockify Tools - Allowlist
# Curated subset of Clockify API for AI agent use.
# Each tool is an allowlist entry: only listed tools are exposed to LLMs.

# === TOOL LIST ===
# 1. clockify_list_time_entries          - List time entries
# 2. clockify_get_time_entry             - Get time entry details
# 3. clockify_create_time_entry          - Create a new time entry
# 4. clockify_update_time_entry          - Update a time entry
# 5. clockify_list_projects              - List projects
# 6. clockify_get_project                - Get project details
# 7. clockify_list_workspaces            - List workspaces
# 8. clockify_get_workspace             - Get workspace details
# 9. clockify_get_time_report            - Get time report
# 10. clockify_list_users                - List users

- slug: clockify_list_time_entries
  name: List Time Entries
  description: |
    List time entries with optional filtering by user, project, or date range.
  provider: clockify
  endpoint: /v1/workspaces/{workspaceId}/time-entries
  method: GET
  scopes:
    - read
  tags:
    - time_entries
    - list
  input_schema:
    type: object
    description: Parameters for listing time entries
    properties:
      workspaceId:
        type: string
        description: "Workspace ID"
      userId:
        type: string
        description: "User ID"
      projectId:
        type: string
        description: "Project ID"
      startDate:
        type: string
        description: "Start date (YYYY-MM-DD)"
      endDate:
        type: string
        description: "End date (YYYY-MM-DD)"
    required:
      - workspaceId

- slug: clockify_get_time_entry
  name: Get Time Entry
  description: |
    Get detailed information about a specific time entry.
  provider: clockify
  endpoint: /v1/workspaces/{workspaceId}/time-entries/{id}
  method: GET
  scopes:
    - read
  tags:
    - time_entries
    - read
  input_schema:
    type: object
    description: Parameters for getting a time entry
    properties:
      workspaceId:
        type: string
        description: "Workspace ID"
      id:
        type: string
        description: "Time entry ID"
    required:
      - workspaceId
      - id

- slug: clockify_create_time_entry
  name: Create Time Entry
  description: |
    Create a new time entry.
  provider: clockify
  endpoint: /v1/workspaces/{workspaceId}/time-entries
  method: POST
  scopes:
    - write
  tags:
    - time_entries
    - create
  input_schema:
    type: object
    description: Parameters for creating a time entry
    properties:
      workspaceId:
        type: string
        description: "Workspace ID"
      projectId:
        type: string
        description: "Project ID"
      description:
        type: string
        description: "Time entry description"
      start:
        type: string
        description: "Start time (ISO 8601)"
      end:
        type: string
        description: "End time (ISO 8601)"
    required:
      - workspaceId

- slug: clockify_update_time_entry
  name: Update Time Entry
  description: |
    Update an existing time entry.
  provider: clockify
  endpoint: /v1/workspaces/{workspaceId}/time-entries/{id}
  method: PUT
  scopes:
    - write
  tags:
    - time_entries
    - update
  input_schema:
    type: object
    description: Parameters for updating a time entry
    properties:
      workspaceId:
        type: string
        description: "Workspace ID"
      id:
        type: string
        description: "Time entry ID"
      projectId:
        type: string
        description: "New project ID"
      description:
        type: string
        description: "New description"
      start:
        type: string
        description: "New start time (ISO 8601)"
      end:
        type: string
        description: "New end time (ISO 8601)"
    required:
      - workspaceId
      - id

- slug: clockify_list_workspaces
  name: List Workspaces
  description: |
    List all workspaces for the authenticated user.
  provider: clockify
  endpoint: /v1/workspaces
  method: GET
  scopes:
    - read
  tags:
    - workspaces
    - list
  input_schema:
    type: object
    description: No parameters required
    properties: {}
    required: []

- slug: clockify_get_workspace
  name: Get Workspace
  description: |
    Get detailed information about a specific workspace.
  provider: clockify
  endpoint: /v1/workspaces/{workspaceId}
  method: GET
  scopes:
    - read
  tags:
    - workspaces
    - read
  input_schema:
    type: object
    description: Parameters for getting a workspace
    properties:
      workspaceId:
        type: string
        description: "Workspace ID"
    required:
      - workspaceId

- slug: clockify_list_projects
  name: List Projects
  description: |
    List all projects in a workspace.
  provider: clockify
  endpoint: /v1/workspaces/{workspaceId}/projects
  method: GET
  scopes:
    - read
  tags:
    - projects
    - list
  input_schema:
    type: object
    description: Parameters for listing projects
    properties:
      workspaceId:
        type: string
        description: "Workspace ID"
      archived:
        type: boolean
        description: "Include archived projects"
    required:
      - workspaceId

- slug: clockify_get_project
  name: Get Project
  description: |
    Get detailed information about a specific project.
  provider: clockify
  endpoint: /v1/workspaces/{workspaceId}/projects/{id}
  method: GET
  scopes:
    - read
  tags:
    - projects
    - read
  input_schema:
    type: object
    description: Parameters for getting a project
    properties:
      workspaceId:
        type: string
        description: "Workspace ID"
      id:
        type: string
        description: "Project ID"
    required:
      - workspaceId
      - id

- slug: clockify_list_users
  name: List Users
  description: |
    List all users in a workspace.
  provider: clockify
  endpoint: /v1/workspaces/{workspaceId}/users
  method: GET
  scopes:
    - read
  tags:
    - users
    - list
  input_schema:
    type: object
    description: Parameters for listing users
    properties:
      workspaceId:
        type: string
        description: "Workspace ID"
    required:
      - workspaceId

- slug: clockify_get_user
  name: Get User
  description: |
    Get detailed information about a specific user.
  provider: clockify
  endpoint: /v1/users/{id}
  method: GET
  scopes:
    - read
  tags:
    - users
    - read
  input_schema:
    type: object
    description: Parameters for getting a user
    properties:
      id:
        type: string
        description: "User ID"
    required:
      - id
