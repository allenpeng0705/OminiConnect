# JazzHR Tools - Allowlist
# Curated subset of JazzHR API for AI agent use.
# API_KEY via Nango.
# Each tool is an allowlist entry: only listed tools are exposed to LLMs.

# === TOOL LIST ===
# 1. jazzhr_list_candidates         - List candidates
# 2. jazzhr_get_candidate           - Get candidate details
# 3. jazzhr_list_jobs               - List job postings
# 4. jazzhr_get_job                 - Get job details
# 5. jazzhr_list_users              - List users
# 6. jazzhr_get_user                - Get user details
# 7. jazzhr_list_notes              - List notes
# 8. jazzhr_create_note             - Create a note
# 9. jazzhr_list_activities         - List activities
# 10. jazzhr_list_questionnaires    - List questionnaires

- slug: jazzhr_list_candidates
  name: List Candidates
  description: |
    List all candidates in JazzHR. Returns candidate names, email addresses, and application status.
  provider: jazzhr
  endpoint: /v1/candidates
  method: GET
  scopes: []
  tags:
    - candidates
    - list
    - read
  input_schema:
    type: object
    description: Parameters for listing candidates
    properties:
      page:
        type: integer
        description: "Page number"
        default: 1
      per_page:
        type: integer
        description: "Results per page"
        default: 20
    required: []

- slug: jazzhr_get_candidate
  name: Get Candidate
  description: |
    Retrieve details about a specific candidate by their ID. Includes contact information, resume, and application history.
  provider: jazzhr
  endpoint: /v1/candidates/{candidate_id}
  method: GET
  scopes: []
  tags:
    - candidates
    - read
  input_schema:
    type: object
    description: Parameters for getting a candidate
    properties:
      candidate_id:
        type: string
        description: "Candidate ID"
    required:
      - candidate_id

- slug: jazzhr_list_jobs
  name: List Jobs
  description: |
    List all job postings in JazzHR. Filter by status (open, closed) and return job titles and posting dates.
  provider: jazzhr
  endpoint: /v1/jobs
  method: GET
  scopes: []
  tags:
    - jobs
    - list
    - read
  input_schema:
    type: object
    description: Parameters for listing jobs
    properties:
      status:
        type: string
        description: "Job status (open, closed, all)"
        default: open
      page:
        type: integer
        description: "Page number"
        default: 1
    required: []

- slug: jazzhr_get_job
  name: Get Job
  description: |
    Retrieve details about a specific job posting by its ID. Includes job description, requirements, and hiring status.
  provider: jazzhr
  endpoint: /v1/jobs/{job_id}
  method: GET
  scopes: []
  tags:
    - jobs
    - read
  input_schema:
    type: object
    description: Parameters for getting a job
    properties:
      job_id:
        type: string
        description: "Job ID"
    required:
      - job_id

- slug: jazzhr_list_users
  name: List Users
  description: |
    List all users (recruiters and administrators) in the JazzHR organization. Returns names, email addresses, and roles.
  provider: jazzhr
  endpoint: /v1/users
  method: GET
  scopes: []
  tags:
    - users
    - list
    - read
  input_schema:
    type: object
    description: Parameters for listing users
    properties: {}
    required: []

- slug: jazzhr_get_user
  name: Get User
  description: |
    Retrieve details about a specific user by their ID. Includes name, email, and role information.
  provider: jazzhr
  endpoint: /v1/users/{user_id}
  method: GET
  scopes: []
  tags:
    - users
    - read
  input_schema:
    type: object
    description: Parameters for getting a user
    properties:
      user_id:
        type: string
        description: "User ID"
    required:
      - user_id

- slug: jazzhr_list_notes
  name: List Notes
  description: |
    List notes associated with candidates or jobs. Filter by candidate ID or job ID and return note content and author.
  provider: jazzhr
  endpoint: /v1/notes
  method: GET
  scopes: []
  tags:
    - notes
    - list
    - read
  input_schema:
    type: object
    description: Parameters for listing notes
    properties:
      candidate_id:
        type: string
        description: "Filter by candidate ID"
      job_id:
        type: string
        description: "Filter by job ID"
    required: []

- slug: jazzhr_create_note
  name: Create Note
  description: |
    Create a new note on a candidate or job. Attach the note to a specific candidate or job posting.
  provider: jazzhr
  endpoint: /v1/notes
  method: POST
  scopes: []
  tags:
    - notes
    - create
    - write
  input_schema:
    type: object
    description: Parameters for creating a note
    properties:
      candidate_id:
        type: string
        description: "Candidate ID"
      job_id:
        type: string
        description: "Job ID"
      body:
        type: string
        description: "Note content"
    required:
      - candidate_id
      - body

- slug: jazzhr_list_activities
  name: List Activities
  description: |
    List activities (events, updates) related to candidates or jobs. Filter by candidate ID or job ID to track timeline.
  provider: jazzhr
  endpoint: /v1/activities
  method: GET
  scopes: []
  tags:
    - activities
    - list
    - read
  input_schema:
    type: object
    description: Parameters for listing activities
    properties:
      candidate_id:
        type: string
        description: "Filter by candidate ID"
      job_id:
        type: string
        description: "Filter by job ID"
    required: []

- slug: jazzhr_list_questionnaires
  name: List Questionnaires
  description: |
    List all questionnaires in JazzHR. Returns questionnaire names and associated job postings.
  provider: jazzhr
  endpoint: /v1/questionnaires
  method: GET
  scopes: []
  tags:
    - questionnaires
    - list
    - read
  input_schema:
    type: object
    description: Parameters for listing questionnaires
    properties: {}
    required: []
