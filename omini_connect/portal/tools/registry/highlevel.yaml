# HighLevel Tools - Allowlist
# Curated subset of HighLevel API for AI agent use.
# OAuth2 via Nango.
# Each tool is an allowlist entry: only listed tools are exposed to LLMs.

# === TOOL LIST ===
# 1. highlevel_list_contacts      - List contacts
# 2. highlevel_get_contact         - Get contact details
# 3. highlevel_list_opportunities - List opportunities
# 4. highlevel_get_opportunity     - Get opportunity details
# 5. highlevel_list_tasks         - List tasks
# 6. highlevel_get_task            - Get task details
# 7. highlevel_list_appointments  - List appointments
# 8. highlevel_get_appointment     - Get appointment details
# 9. highlevel_list_campaigns     - List campaigns
# 10. highlevel_list_forms         - List forms

- slug: highlevel_list_contacts
  name: List Contacts
  description: |
    Retrieve all contacts in your HighLevel account with pagination.
    Returns contact names, emails, and status information.
  provider: highlevel
  endpoint: /contacts
  method: GET
  scopes:
    - read
  tags:
    - contacts
    - list
  input_schema:
    type: object
    description: Parameters for listing contacts
    properties:
      page:
        type: integer
        description: "Page number"
        default: 1
    required: []

- slug: highlevel_get_contact
  name: Get Contact
  description: |
    Retrieve a specific HighLevel contact by ID with profile, tags, and pipeline.
    Returns full contact details including custom fields and notes.
  provider: highlevel
  endpoint: /contacts/{id}
  method: GET
  scopes:
    - read
  tags:
    - contacts
    - read
  input_schema:
    type: object
    description: Parameters for getting a contact
    properties:
      id:
        type: string
        description: "Contact ID"
    required:
      - id

- slug: highlevel_list_opportunities
  name: List Opportunities
  description: |
    Retrieve all opportunities in your HighLevel account with optional pipeline filtering.
    Returns opportunity names, values, and pipeline stages.
  provider: highlevel
  endpoint: /opportunities
  method: GET
  scopes:
    - read
  tags:
    - opportunities
    - list
  input_schema:
    type: object
    description: Parameters for listing opportunities
    properties:
      pipeline_id:
        type: string
        description: "Filter by pipeline ID"
      page:
        type: integer
        description: "Page number"
        default: 1
    required: []

- slug: highlevel_get_opportunity
  name: Get Opportunity
  description: |
    Retrieve a specific HighLevel opportunity by ID with its contact and details.
    Returns full opportunity details including stage and monetary value.
  provider: highlevel
  endpoint: /opportunities/{id}
  method: GET
  scopes:
    - read
  tags:
    - opportunities
    - read
  input_schema:
    type: object
    description: Parameters for getting an opportunity
    properties:
      id:
        type: string
        description: "Opportunity ID"
    required:
      - id

- slug: highlevel_list_tasks
  name: List Tasks
  description: |
    Retrieve all tasks in your HighLevel account with optional contact filtering.
    Returns task titles, statuses, and assignee information.
  provider: highlevel
  endpoint: /tasks
  method: GET
  scopes:
    - read
  tags:
    - tasks
    - list
  input_schema:
    type: object
    description: Parameters for listing tasks
    properties:
      contact_id:
        type: string
        description: "Filter by contact ID"
      page:
        type: integer
        description: "Page number"
        default: 1
    required: []

- slug: highlevel_get_task
  name: Get Task
  description: |
    Retrieve a specific HighLevel task by ID with its details and due date.
    Returns full task information including status and description.
  provider: highlevel
  endpoint: /tasks/{id}
  method: GET
  scopes:
    - read
  tags:
    - tasks
    - read
  input_schema:
    type: object
    description: Parameters for getting a task
    properties:
      id:
        type: string
        description: "Task ID"
    required:
      - id

- slug: highlevel_list_appointments
  name: List Appointments
  description: |
    Retrieve all appointments in your HighLevel account with date range filtering.
    Returns appointment times, statuses, and service types.
  provider: highlevel
  endpoint: /appointments
  method: GET
  scopes:
    - read
  tags:
    - appointments
    - list
  input_schema:
    type: object
    description: Parameters for listing appointments
    properties:
      start_date:
        type: string
        description: "Start date (YYYY-MM-DD)"
      end_date:
        type: string
        description: "End date (YYYY-MM-DD)"
      page:
        type: integer
        description: "Page number"
        default: 1
    required: []

- slug: highlevel_get_appointment
  name: Get Appointment
  description: |
    Retrieve a specific HighLevel appointment by ID with attendees and details.
    Returns full appointment information including location and notes.
  provider: highlevel
  endpoint: /appointments/{id}
  method: GET
  scopes:
    - read
  tags:
    - appointments
    - read
  input_schema:
    type: object
    description: Parameters for getting an appointment
    properties:
      id:
        type: string
        description: "Appointment ID"
    required:
      - id

- slug: highlevel_list_campaigns
  name: List Campaigns
  description: |
    Retrieve all marketing campaigns in your HighLevel account.
    Returns campaign names and current status.
  provider: highlevel
  endpoint: /campaigns
  method: GET
  scopes:
    - read
  tags:
    - campaigns
    - list
  input_schema:
    type: object
    description: Parameters for listing campaigns
    properties: {}
    required: []

- slug: highlevel_list_forms
  name: List Forms
  description: |
    Retrieve all forms in your HighLevel account.
    Returns form names and submission counts.
  provider: highlevel
  endpoint: /forms
  method: GET
  scopes:
    - read
  tags:
    - forms
    - list
  input_schema:
    type: object
    description: Parameters for listing forms
    properties: {}
    required: []
