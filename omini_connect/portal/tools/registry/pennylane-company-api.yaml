# Pennylane Company API Tools - Allowlist
# Curated subset of Pennylane Company API for AI agent use.
# API Key via Nango.
# Each tool is an allowlist entry: only listed tools are exposed to LLMs.
# NOTE: pennylane-company-api uses the same API endpoints as pennylane - only auth is different.

# === TOOL LIST ===
# 1. pennylane_company_list_accounts            - List accounts
# 2. pennylane_company_get_account             - Get account details
# 3. pennylane_company_list_contacts           - List contacts
# 4. pennylane_company_get_contact            - Get contact details
# 5. pennylane_company_list_invoices           - List invoices
# 6. pennylane_company_get_invoice            - Get invoice details
# 7. pennylane_company_list_expenses           - List expenses
# 8. pennylane_company_get_expense            - Get expense details
# 9. pennylane_company_list_items             - List items/products
# 10. pennylane_company_get_company_info      - Get company information

- slug: pennylane_company_list_accounts
  name: List Accounts
  description: |
    List all accounts in the accounting system.
  provider: pennylane-company-api
  endpoint: /api/v1/accounts
  method: GET
  scopes: []
  tags:
    - accounts
    - accounting
    - list
  input_schema:
    type: object
    description: Parameters for listing accounts
    properties:
      type:
        type: string
        description: "Filter by account type (asset, liability, equity, revenue, expense)"
    required: []

- slug: pennylane_company_get_account
  name: Get Account
  description: |
    Get detailed information about a specific account.
  provider: pennylane-company-api
  endpoint: /api/v1/accounts/{accountId}
  method: GET
  scopes: []
  tags:
    - accounts
    - accounting
    - read
  input_schema:
    type: object
    description: Parameters for getting an account
    properties:
      accountId:
        type: string
        description: "Account ID"
    required:
      - accountId

- slug: pennylane_company_list_contacts
  name: List Contacts
  description: |
    List all contacts (customers and suppliers).
  provider: pennylane-company-api
  endpoint: /api/v1/contacts
  method: GET
  scopes: []
  tags:
    - contacts
    - crm
    - list
  input_schema:
    type: object
    description: Parameters for listing contacts
    properties:
      type:
        type: string
        description: "Filter by type (customer, supplier)"
    required: []

- slug: pennylane_company_get_contact
  name: Get Contact
  description: |
    Get detailed information about a specific contact.
  provider: pennylane-company-api
  endpoint: /api/v1/contacts/{contactId}
  method: GET
  scopes: []
  tags:
    - contacts
    - crm
    - read
  input_schema:
    type: object
    description: Parameters for getting a contact
    properties:
      contactId:
        type: string
        description: "Contact ID"
    required:
      - contactId

- slug: pennylane_company_list_invoices
  name: List Invoices
  description: |
    List all invoices.
  provider: pennylane-company-api
  endpoint: /api/v1/invoices
  method: GET
  scopes: []
  tags:
    - invoices
    - billing
    - list
  input_schema:
    type: object
    description: Parameters for listing invoices
    properties:
      status:
        type: string
        description: "Filter by status (draft, sent, paid, cancelled)"
    required: []

- slug: pennylane_company_get_invoice
  name: Get Invoice
  description: |
    Get detailed information about a specific invoice.
  provider: pennylane-company-api
  endpoint: /api/v1/invoices/{invoiceId}
  method: GET
  scopes: []
  tags:
    - invoices
    - billing
    - read
  input_schema:
    type: object
    description: Parameters for getting an invoice
    properties:
      invoiceId:
        type: string
        description: "Invoice ID"
    required:
      - invoiceId

- slug: pennylane_company_list_expenses
  name: List Expenses
  description: |
    List all expenses.
  provider: pennylane-company-api
  endpoint: /api/v1/expenses
  method: GET
  scopes: []
  tags:
    - expenses
    - accounting
    - list
  input_schema:
    type: object
    description: Parameters for listing expenses
    properties:
      status:
        type: string
        description: "Filter by status (draft, validated)"
      startDate:
        type: string
        description: "Start date filter (YYYY-MM-DD)"
      endDate:
        type: string
        description: "End date filter (YYYY-MM-DD)"
    required: []

- slug: pennylane_company_get_expense
  name: Get Expense
  description: |
    Get detailed information about a specific expense.
  provider: pennylane-company-api
  endpoint: /api/v1/expenses/{expenseId}
  method: GET
  scopes: []
  tags:
    - expenses
    - accounting
    - read
  input_schema:
    type: object
    description: Parameters for getting an expense
    properties:
      expenseId:
        type: string
        description: "Expense ID"
    required:
      - expenseId

- slug: pennylane_company_list_items
  name: List Items
  description: |
    List all items/products in the catalog.
  provider: pennylane-company-api
  endpoint: /api/v1/items
  method: GET
  scopes: []
  tags:
    - items
    - products
    - list
  input_schema:
    type: object
    description: Parameters for listing items
    properties: {}
    required: []

- slug: pennylane_company_get_company_info
  name: Get Company Info
  description: |
    Get company/organization information.
  provider: pennylane-company-api
  endpoint: /api/v1/company
  method: GET
  scopes: []
  tags:
    - company
    - organization
    - read
  input_schema:
    type: object
    description: Parameters for getting company info
    properties: {}
    required: []
