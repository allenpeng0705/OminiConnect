# Incident.io Tools - Allowlist
# Curated subset of Incident.io API for AI agent use.
# API Key via Nango.
# Each tool is an allowlist entry: only listed tools are exposed to LLMs.

# === TOOL LIST ===
# 1. incident_list_incidents       - List incidents
# 2. incident_get_incident         - Get incident details
# 3. incident_list_severities     - List severities
# 4. incident_list_urgencies      - List urgencies
# 5. incident_list_status_pages   - List status pages
# 6. incident_list_members        - List members
# 7. incident_list_roles          - List roles
# 8. incident_list_catalog         - List catalog items
# 9. incident_get_catalog         - Get catalog item details
# 10. incident_list_metrics        - List metrics

- slug: incident_list_incidents
  name: List Incidents
  description: |
    Retrieve all incidents in your incident.io account with optional status filtering.
    Returns incident titles, statuses, severities, and creation timestamps.
  provider: incident-io
  endpoint: /v1/incidents
  method: GET
  scopes:
    - read
  tags:
    - incidents
    - list
  input_schema:
    type: object
    description: Parameters for listing incidents
    properties:
      status:
        type: string
        description: "Filter by status: investigating, identified, monitoring, resolved"
      page:
        type: integer
        description: "Page number"
        default: 1
    required: []

- slug: incident_get_incident
  name: Get Incident
  description: |
    Retrieve a specific incident by ID with full timeline and affected services.
    Returns incident details including status, severity, timeline events, and commander.
  provider: incident-io
  endpoint: /v1/incidents/{id}
  method: GET
  scopes:
    - read
  tags:
    - incidents
    - read
  input_schema:
    type: object
    description: Parameters for getting an incident
    properties:
      id:
        type: string
        description: "Incident ID"
    required:
      - id

- slug: incident_list_severities
  name: List Severities
  description: |
    Retrieve all severity levels configured in your incident.io account.
    Returns severity names, ranks, and descriptions.
  provider: incident-io
  endpoint: /v1/severities
  method: GET
  scopes:
    - read
  tags:
    - severities
    - list
  input_schema:
    type: object
    description: Parameters for listing severities
    properties: {}
    required: []

- slug: incident_list_urgencies
  name: List Urgencies
  description: |
    Retrieve all urgency levels configured in your incident.io account.
    Returns urgency names, ranks, and response time targets.
  provider: incident-io
  endpoint: /v1/urgencies
  method: GET
  scopes:
    - read
  tags:
    - urgencies
    - list
  input_schema:
    type: object
    description: Parameters for listing urgencies
    properties: {}
    required: []

- slug: incident_list_status_pages
  name: List Status Pages
  description: |
    Retrieve all status pages in your incident.io account for public incident communication.
    Returns status page names, subdomains, and current status.
  provider: incident-io
  endpoint: /v1/status-pages
  method: GET
  scopes:
    - read
  tags:
    - status_pages
    - list
  input_schema:
    type: object
    description: Parameters for listing status pages
    properties: {}
    required: []

- slug: incident_list_members
  name: List Members
  description: |
    Retrieve all team members in your incident.io account with roles and contact info.
    Returns member names, emails, roles, and on-call status.
  provider: incident-io
  endpoint: /v1/members
  method: GET
  scopes:
    - read
  tags:
    - members
    - list
  input_schema:
    type: object
    description: Parameters for listing members
    properties: {}
    required: []

- slug: incident_list_roles
  name: List Roles
  description: |
    Retrieve all incident roles defined in your incident.io account for incident management.
    Returns role names, descriptions, and default assignments.
  provider: incident-io
  endpoint: /v1/roles
  method: GET
  scopes:
    - read
  tags:
    - roles
    - list
  input_schema:
    type: object
    description: Parameters for listing roles
    properties: {}
    required: []

- slug: incident_list_catalog
  name: List Catalog
  description: |
    Retrieve all catalog items in your incident.io account with optional type filtering.
    Returns catalog entries including services, teams, and their relationships.
  provider: incident-io
  endpoint: /v1/catalog
  method: GET
  scopes:
    - read
  tags:
    - catalog
    - list
  input_schema:
    type: object
    description: Parameters for listing catalog items
    properties:
      type:
        type: string
        description: "Filter by type"
    required: []

- slug: incident_get_catalog
  name: Get Catalog Item
  description: |
    Retrieve a specific catalog item by ID with full properties and linked services.
    Returns catalog entry details including metadata, dependencies, and owner information.
  provider: incident-io
  endpoint: /v1/catalog/{id}
  method: GET
  scopes:
    - read
  tags:
    - catalog
    - read
  input_schema:
    type: object
    description: Parameters for getting a catalog item
    properties:
      id:
        type: string
        description: "Catalog item ID"
    required:
      - id

- slug: incident_list_metrics
  name: List Metrics
  description: |
    Retrieve all incident metrics configured in your incident.io account for reporting.
    Returns metric names, types, and calculation formulas.
  provider: incident-io
  endpoint: /v1/metrics
  method: GET
  scopes:
    - read
  tags:
    - metrics
    - list
  input_schema:
    type: object
    description: Parameters for listing metrics
    properties: {}
    required: []
